{
	"name": "<%= packageName %>",
	"version": "0.0.1",
	"main": "./index.js",
	"scripts": {
		"dev": "export NODE_ENV=development && node index.js"
	},
	"keywords": [
		"koa"
	],
	"author": "ran",
	"homepage": "https://github.com/zhangran963",
	"license": "ISC",
	"dependencies": {
    "koa": "^2.13.1"
    <% if (middleware.body) { %>
      ,"koa-body": "^4.1.3"
    <% } %>
    <% if (middleware.router) { %>
      ,"koa-router": "^8.0.8"
    <% } %>
    <% if (middleware.static) { %>
      ,"koa-static": "^5.0.0"
    <% } %>
    <% if (middleware.views) { %>
      ,"koa-views": "^6.2.2"
    <% } %>
	},
	"devDependencies": {
		"@types/jest": "^25.2.3",
		"jest": "^26.0.1"
	}
}
